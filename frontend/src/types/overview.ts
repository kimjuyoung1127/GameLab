/** 대시보드 개요 타입: OverviewMetrics 인터페이스. */
export interface OverviewMetrics {
  totalSessions: number;
  totalFiles: number;
  filesProcessed: number;
  avgAccuracy: number;
  recentUploads: number;
  activeSessions: number;
}
